# Adding "pyshtools" on top of base
# Includes Jupyter

FROM buddhasystem/lusee-night-base:0.1
WORKDIR /app
COPY docker/requirements-pyshtools.txt requirements-pyshtools.txt
RUN pip install -r requirements-pyshtools.txt

